#include <vector>
#include <string>
using namespace std;

class Solution {
  public:
    vector<int> partitionLabels(string s) {
    }

  public:
    int getLetterIndex(char letter) {
      return 0;
    }
};
